# API keys
.env

# Virtual environment
venv/

# ChromaDB databases

test_db/

# Carrier PDFs (too large for GitHub)
carrier_docs/
Carrier_Eligibility_PDFs/
carrier_docs_db/
!carrier_docs_db_seed/
!carrier_docs_db_seed/**

# Python cache
__pycache__/
*.pyc

# Ad-hoc experiment run state (resumable scratch state, not source of truth)
verification/*_results.json
verification/isolation_ab_*.json

# Raw model response dumped on a JSON parse failure (runtime diagnostic)
last_json_parse_failure.txt

# Raw sweep run data (large scratch state). Condensed, committable
# per-carrier baselines live in verification/baselines/ instead.
verification/sweep_*.json
